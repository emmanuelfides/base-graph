{
  "name": "base-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ base-graph",
    "create-local": "graph create --node http://localhost:8020/ base-graph",
    "remove-local": "graph remove --node http://localhost:8020/ base-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 base-graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.80.0",
    "@graphprotocol/graph-ts": "0.32.0",
    "ethers": "^6.13.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
